**Installation**

```js
// All Project Installation
npm install

// In addition to, We need to install @babel/node as global to run server in development mode
npm install @babel/cli @babel/core @babel/node -g
```
